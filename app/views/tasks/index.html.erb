<div class="row">
  <div class="one-half column">
    <ul class="task-list">
      <% @tasks.each do |task| %>
        <%= render task %>
      <% end %>
    </ul>
  </div>
  <div class="one-half column">
    <%= render 'form' %>
  </div>
</div>
